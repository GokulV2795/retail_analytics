name: retail_analytics
version: "1.0.0"
config-version: 2

profile: retail_analytics

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]
target-path: "target"
clean-targets:

* "target"
* "dbt_packages"

models:
retail_analytics:
bronze:
+materialized: view
+schema: bronze
+tags: ["bronze", "retail"]

```
silver:
  +materialized: table
  +schema: silver
  +tags: ["silver", "retail"]

gold:
  +materialized: table
  +schema: gold
  +tags: ["gold", "bi", "ai", "retail"]
```

seeds:
retail_analytics:
+schema: raw
+quote_columns: false
